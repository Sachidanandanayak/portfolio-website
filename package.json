{
  "name": "portfolio-website-root",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "cd portfolio-website && npm install && npm run build && node -e \"require('fs').cpSync('portfolio-website/dist', 'dist', {recursive: true})\""
  }
}
